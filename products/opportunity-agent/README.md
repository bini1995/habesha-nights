# Opportunity Agent

This directory marks the boundary of the existing monitoring product.

The working implementation remains at the repository root for now so its
Docker image, dashboard, API routes, scheduler, snapshots, events, email
alerts, and AMC provider continue to work unchanged. It will move here only
after compatibility coverage is in place.
